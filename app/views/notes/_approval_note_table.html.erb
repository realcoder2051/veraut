<div class="table-responsive border-bottom" data-toggle="lists" data-lists-values='["js-lists-values-employee-name"]'>
	<table class="table mb-0 thead-border-top-0 mt-1">
		<thead>
			<tr class="table-info">
				<th>Note</th>
				<th>Date</th>
				<th>Created_by</th>
				<th colspan="1">Actions</th>
			</tr>
		</thead>
		<% count = 0 %>
		<tbody class="list" id="blah">
			<% @notes.each do |note| %>
			<% if (note.created_by == current_user.email) %>
			<% count += 1 %>
			<!-- <tr class ="redirect_to_show cursor-pointer" data-url="<%= note_path(note) %>">
						<td><%= note.description%></td>
						<td><%= note.created_at %></td>
						<td><%= note.created_by %></td>  
						<td>
						<a href=<%=edit_note_path(note)%> ><i class="material-icons icon-20pt modal-edit">edit</i></a>
						<a data-confirm="Are you sure?" rel="nofollow" data-method="delete" href=<%=note_path(note)%>><i class="material-icons">delete</i></a>
						</td>
					</tr> -->
			<%= render 'notes/approval_note', note: note %>
			<% end %>
			<% end %>
			<% if count == 0 %>
			<tr>
				<td> no results found </td>
			</tr>
			<%end%>
		</tbody>
	</table>
</div>