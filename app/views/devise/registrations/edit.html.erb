<div class="mx-auto mt-2 border shadow w-1/3">
  <div class="flex shadow bg-gray-100">
    <span class="text-xl text-bold text-gray-400 mx-auto my-3">Change your Password</span>
  </div>

  <h2 class="text-lg ml-4 mt-3 font-bold">Edit <%= resource_name.to_s.humanize %>
  </h2>
  <div class="flex items-center">
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
      <%= render "devise/shared/error_messages" , resource: resource %>
        <div class="flex">
          <div class="field ml-16">
            <%= f.label :username, class: 'block ml-1 text-gray-700 text-base font-bold mt-5' %>
              <%= f.text_field :username, autofocus: true, autocomplete: "username", class: 'shadow ml-1 appearance-none border rounded w-72 py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline', disabled: "disabled" %>
          </div>
        </div>

        <div class="flex">
          <div class="field ml-16">
            <%= f.label :current_password, class: 'block ml-1 text-gray-700 text-base font-bold mt-5' %> <i class="text-sm mr-2">(we need your current password to confirm your changes)</i><br />
            <%= f.password_field :current_password, autocomplete: "current-password", class: 'shadow ml-1 appearance-none border rounded w-72 py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline' %>
          </div>
        </div>

        <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
          <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
        <% end %>
        <div class="flex">
          <div class="field ml-16">
            <%= f.label :password, "New Password", class: 'block ml-1 text-gray-700 text-base font-bold mt-5' %> <div class="text-sm">(leave blank if you don't want to change it)</div>
            <%= f.password_field :password, autocomplete: "new-password", class: 'shadow ml-1 appearance-none border rounded w-72 py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline' %>
              <% if @minimum_password_length %>
                <div>
                  <em class="text-sm"><%= @minimum_password_length %> characters minimum</em>
                </div>
              <% end %>
          </div>
        </div>

        <div class="flex">
          <div class="field ml-16">
            <%= f.label :password_confirmation, "New Password Confirmation", class: 'block ml-1 text-gray-700 text-base font-bold mt-5' %>
              <%= f.password_field :password_confirmation, autocomplete: "new-password", class: 'shadow ml-1 appearance-none border rounded w-72 py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline' %>
          </div>
        </div>

        <!--<div class="flex">
          <div class="field ml-16">
            <%= f.label :current_password, class: 'block ml-1 text-gray-700 text-base font-bold mt-5' %> <i class="text-sm">(we need your current password to confirm your changes)</i><br />
              <%= f.password_field :current_password, autocomplete: "current-password", class: 'shadow ml-1 appearance-none border rounded w-72 py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline' %>
          </div>
        </div> -->
        <div class="ml-5 mb-5 hidden password-message">
          <div class="mt-2 flex">
            <img src="<%= image_url('right.svg') %>" class="characters-right hidden h-5 mt-1 mr-2" />
            <img src="<%= image_url('cross.svg') %>" class="characters-cross hidden h-5 mt-1 mr-2" />
            <span class="font-bold length-of-characters">Use 8 or more characters</span>
          </div>
          <div class="mt-2 flex">
            <img src="<%= image_url('right.svg') %>" class="upper-lower-right hidden h-5 mt-1 mr-2" />
            <img src="<%= image_url('cross.svg') %>" class="upper-lower-cross hidden h-5 mt-1 mr-2" />
            <span class="font-bold upper-lower-letters">Use upper and lower case letters (e.g. Aa)</span>
          </div>
          <div class="mt-2 flex">
            <img src="<%= image_url('right.svg') %>" class="numbers-right hidden h-5 mt-1 mr-2" />
            <img src="<%= image_url('cross.svg') %>" class="numbers-cross hidden h-5 mt-1 mr-2" />
            <span class="font-bold number-message">Use a number (e.g. 1234)</span>
          </div>
          <div class="mt-2 flex">
            <img src="<%= image_url('right.svg') %>" class="symbol-right hidden h-5 mt-1 mr-2" />
            <img src="<%= image_url('cross.svg') %>" class="symbol-cross hidden h-5 mt-1 mr-2" />
            <span class="font-bold symbol-message">Use a symbol (e.g. !@#$)</span>
          </div>
        </div>

        <div class="flex">
          <div class="flex mx-auto">
            <div class="actions mr-4 mb-10">
              <%= f.submit "Update" , class:"dark-blue password-reset-button focus:outline-none text-white text-base py-2.5 px-3 rounded-md hover:shadow-lg mt-5" %>
            </div>
            <div class="mt-5 py-2.5">
              <%= link_to "Back" , account_settings_dashboards_path, class:"focus:outline-none text-white text-base py-3 px-3 rounded-md bg-yellow-500 hover:bg-yellow-600 hover:shadow-lg" %>
            </div>
          </div>
        </div>
    <% end %>
  </div>
</div>
<!--<h3>Cancel my account</h3>-->
<!--<p>Unhappy? <%= button_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete %></p>-->
