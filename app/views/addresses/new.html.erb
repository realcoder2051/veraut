	<div class="container-fluid page__heading-container">
		<div class="page__heading cancel-row">
			<nav aria-label="breadcrumb">
				<ol class="breadcrumb mb-0">
					<li class="breadcrumb-item"><i class="material-icons icon-20pt">home</i></li>
					<li class="breadcrumb-item active" aria-current="page">Address</a></li>
					<li class="breadcrumb-item active" aria-current="page">New Address</li>
				</ol>
			</nav>
			<h1 class="m-0">Add Address </h1>
		</div>
		<div class="container-fluid page__container">
			<div class="card card-">
				<div class="row no-gutters">
					<div class="col-lg-12 card-form__body card-body">
						<%= bootstrap_form_for(@address, local:true) do |f| %>
							<% if flash[:alert]%>
								<%flash[:alert].split('.').each do |error|%>
									<div class="company_flash_message">
										<div class="container flash-custom" >
											<%=error%>
										</div>
									</div>
								<%end%>
								<% flash[:alert]=nil%>
							<%end%>
							<%=render 'select_form', f: f %>
						<% end %>
					</div>
				</div>
			</div>
		</div>
	</div>
