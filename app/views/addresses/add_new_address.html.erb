	<div class="container-fluid page__heading-container">
		<div class="page__heading cancel-row">
			<nav aria-label="breadcrumb">
				<ol class="breadcrumb mb-0">
					<li class="breadcrumb-item"><i class="material-icons icon-20pt">home</i></li>
					<li class="breadcrumb-item active" aria-current="page">Address</a></li>
					<li class="breadcrumb-item active" aria-current="page">New Address</li>
				</ol>
			</nav>
			<h1 class="m-0">Add New Address </h1>
		</div>
		<div class="container-fluid page__container">
			<div class="card card-">
				<div class="row no-gutters">
					<div class="col-lg-12 card-form__body card-body">
						<%= bootstrap_form_for(@address,:url => { :action => "create_new_address" }, local:true) do |f| %>
							<% if @address.errors.any? %>
							<div id="error_explanation">
								<h2><%= pluralize(@address.errors.count, "error") %> prohibited this user from being saved:</h2>
								<ul>
									<% @address.errors.full_messages.each do |message| %>
									<li><%=message %></li>
									<% end %>
								</ul>
							</div>
							<% end %>
							<%=render 'form', f: f %>
						<% end %>
					</div>
				</div>
			</div>
		</div>
	</div>
