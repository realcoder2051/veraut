<div class="container-fluid full-scroll">
 <div class="page__heading cancel-row">
    <nav aria-label="breadcrumb">
      <ol class="breadcrumb mb-0">
        <li class="breadcrumb-item"><i class="material-icons icon-20pt">home</i></li>
        <li class="breadcrumb-item active" aria-current="page">User</a></li>
      </ol>
    </nav>
    <div style="display:inline-block" class=" col-md-4  w-50 p-12 px-md-1 ">
     <h1>View User</h1> <%=link_to 'Add New Role',new_role_path, class:"btn btn-primary" %>
    </div>
  </div>
  <table class="table">
    <thead class="thead-dark">
      <tr>
        <th>Role</th>
        <th colspan="1">Actions</th>
      </tr>
    </thead>
    <tbody>
      <% @roles.each do |role| %>
        <tr>
          <td><%= role.name %></td>
          <td>
          <i class="material-icons icon-20pt"><%= link_to 'edit', edit_role_path(role) %></i>
          <i class="material-icons icon-20pt"><%= link_to 'delete', role, method: :delete, data: { confirm: 'Are you sure?' } %></i></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
<br>
