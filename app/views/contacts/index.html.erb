<div class="employee_index_scroll">
	<div class="container-fluid page__heading-container">
		<div class="page__heading d-flex align-items-center">
			<div class="flex">
				<div class="row">
					<div class="col-lg-2">
						<nav aria-label="breadcrumb">
							<ol class="breadcrumb mb-0">
								<li class="breadcrumb-item"><i class="material-icons icon-20pt">home</i></li>
								<li class="breadcrumb-item active" aria-current="page">Contact</a></li>
							</ol>
							<h1 class="m-0" <%= count()%>>Contact</h1>
						</nav>
					</div>
					<!-- Stepper partial -->
					<div class="container">
						<div>
							<%= render '/shared/stepper' %>
						</div>
					</div>
				</div>
			</div>
		</div>
	</div>

	<div class="container-fluid page__container">
		<div>
			<h3 class="ml-1"> Step 6 : Contacts :
				<div class="ml-3 mr-2 my-1 float-right">
					<%=link_to "Save",save_contact_path,class: "btn btn-success " %>
				</div>
				<div class="ml-3 my-1 float-right">
					<button type="button" class="btn btn-success  px-3 notes_count" data-toggle="modal"
						data-target="#myModal ">Notes <%= [no_of_notes(controller_name.singularize)] %> </button>
				</div>
			</h3>
		</div>
		<div class="mt-4 mb-2 ml-1">
			<span style="font-style: italic;"> Please verify the list of contacts that are associated with your plan. To
				notify us of change that need to
				be made, click the Add button in the upper right corner of the grid to notify your change. When you have
				completed your change request. Once you have reviewed the list, click the Save button.</span>
		</div>
		<div class="card card-form">
			<div class="row no-gutters">
				<div class="col-lg-12 card-form__body">
					<div class="table-responsive pl-2 pr-2">
						<table class="table mb-0 thead-border-top-0 mt-1  table-hover">
							<thead class="table-info">
								<tr>
									<th><%= sort_link(@q, :username,'Name') %></th>
									<th><%= sort_link(@q, :company_name) %></th>
									<th><%= sort_link(@q, :email,'Email') %></th>
									<th><%= sort_link(@q, :role_id,'Role') %></th>
									<th style="display:none;">Actions</th>
								</tr>
							</thead>
							<%if session[:task_id]%>
							<tbody>
								<tr>
									<td><%=current_user.username%></td>
									<td><%=@name%></td>
									<td><%=current_user.email%></td>
									<td><%=current_user.role&.name%></td>
									<td style="display:none;">
										<%=link_to "Add[0]",new_contact_path(user_id: current_user),class: "btn btn-secondary disabled"%>
									</td>
								</tr>
							</tbody>
							<%end%>
						</table>
					</div>
				</div>
			</div>
		</div>
	</div>
</div>
</div>