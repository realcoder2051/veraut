<div class="container-fluid full-scroll">
	<div class="page__heading cancel-row">
		<nav aria-label="breadcrumb">
			<ol class="breadcrumb mb-0">
				<li class="breadcrumb-item"><i class="material-icons icon-20pt">home</i></li>
				<li class="breadcrumb-item active" aria-current="page">Contact</a></li>
			</ol>
		</nav>
		<div class="row">
			<div style="display:inline-block" class=" col-lg-4  w-50 p-12 px-md-1 ">
				<h1 class="ml-3">View Contact</h1>
			</div>
		</div>
	</div>
	<div class="container-fluid page__container">
		<div>
			<h3> Step 6 : Contacts :
				<div class="ml-3 my-1 float-right">
					<%=link_to "Save",save_contact_path,class: "btn btn-success " %>
				</div> 
				<div class="ml-3 my-1 float-right">
					<button type="button" class="btn btn-success  px-3" data-toggle="modal" data-target="#myModal ">Notes </button>
				</div> </h3>
		</div>
		<div class="mt-4 mb-2">
			<span style="font-style: italic;"> Please verify the list of contacts that are associated with your plan. To notify us of change that need to
				be made, click the Add button in the upper right corner of the grid to notify your change.When you have
				completed your change request. Once you have reviewed the list, click the Save button.</span>
		</div>
	</div>
	<div class="card card-form">
		<div class="row no-gutters">
			<div class="col-lg-12 card-form__body">
				<table class="table mb-0 thead-border-top-0 mt-1  table-hover">
					<thead class="table-info">
						<tr>
							<th><%= sort_link(@q, :name) %></th>
							<th><%= sort_link(@q, :company_name) %></th>
							<th><%= sort_link(@q, :email) %></th>
							<th><%= sort_link(@q, :roles) %></th>
							<th><%= sort_link(@q, :rights) %></th>
							<th>Actions</th>
						</tr>
					</thead>
					<%if session[:task_id]%>
						<tbody>
							<% @users.each do |user|%>
								<tr>
									<td><%=user.username%></td>
									<td><%="Benefit Equity, Inc"%></td>
									<td><%=user.email%></td>
									<td><%=user.role.name%></td>
									<td><%=find_rights(user.role)%></td>
									<td>
										<% if is_already_requested(user)%>
											<%=link_to "View["+total_view_requests.to_s+"]",view_requests_path,class: "btn btn-primary"%>
										<%else%>
											<%=link_to "Add[0]",new_contact_path(user_id: user),class: "btn btn-primary"%>
										<%end%>
									</td>
								</tr>
							<%end%>
						</tbody>
					<%end%>
				</table>
			</div>
		</div>
	</div>
</div>